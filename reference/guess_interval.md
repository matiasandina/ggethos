# Guess Axis Interval

\`r lifecycle::badge("experimental")\` This function guesses the
interval using differences in the x axis provided to geom_ethogram().

## Usage

``` r
guess_interval(diffs)
```
