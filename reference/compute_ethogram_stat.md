# Compute ethogram

\`r lifecycle::badge("experimental")\`

## Usage

``` r
compute_ethogram_stat(
  data,
  scales,
  align_trials,
  remove_nas,
  mode = c("auto", "intervals", "samples", "implied"),
  interval = NULL,
  interval_mode = c("guess", "explicit"),
  align_by = NULL,
  align_mode = c("zero")
)
```
