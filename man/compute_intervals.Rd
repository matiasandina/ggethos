% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute.R
\name{compute_intervals}
\alias{compute_intervals}
\title{Compute Ethogram from Intervals}
\usage{
compute_intervals(
  data,
  x,
  xend,
  y,
  behaviour = NULL,
  group = NULL,
  colour = NULL,
  remove_nas = TRUE
)
}
\arguments{
\item{data}{A data frame.}

\item{x}{Column name for start times.}

\item{xend}{Column name for end times.}

\item{y}{Column name for the y axis.}

\item{behaviour}{Column name for behaviour labels.}

\item{group}{Column name for grouping (optional).}

\item{colour}{Column name for colour (optional).}

\item{remove_nas}{Remove rows with `NA` behaviour values.}
}
\description{
`r lifecycle::badge("experimental")`
}
\details{
Intervals mode is strict about time columns:
\itemize{
  \item `x` and `xend` must be numeric or datetime (POSIXct/Date/difftime).
  \item `x` and `xend` must be complete (no missing or non-finite values).
  \item `xend < x` is an error; `xend == x` emits a warning.
}
}
\examples{
\if{FALSE}{
  df <- data.frame(x = c(1, 2), xend = c(2, 1), y = 1, behaviour = c("a", "b"))
  compute_intervals(df, x, xend, y, behaviour) # error: xend < x
}
}
